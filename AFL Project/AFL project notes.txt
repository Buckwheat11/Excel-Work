AFL project

no data cleaning needed

made a winner column with this IF formula  =IF(P2>V2,K2, IF(P2<V2,Q2, "Draw"))
-- so if home team score grater than away, display hoem team name (dynamic). if away team score greater than home team, display away team name, anythign else is draw

attendance category column  using   =IF(J2<30000,"Low", IF(J2<50000,"Medium","High"))
if less than 30,000 low, if less than 50,0000 but more than 30,000 medium. anythign lese is high 

winner type column   =IF(P2>V2,"Home Team", IF(P2<V2,"Away Team","Draw"))
basically same as other one but instead of displaying team name dyamniucally, hard coded for home team, away , draw


press on pivot chart, pivot chart nalayse, then field buttons, hide axis and value field buttons

turn off get pviot data by selecting pivot table analyse and options to turn it off. to link the kpi cards to pivot tables

when copying piuvot table data into a kpi slot on same sheet, used this after dispalying get pivot data  =TEXT(A127,"#,##") -- must use "#,##"

dataset:  https://www.kaggle.com/datasets/stoney71/aflstats

-- Helpful tutorial

https://www.youtube.com/watch?v=1ic8E58Bo2M
